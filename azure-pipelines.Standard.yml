# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines_Standard --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

stages:
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: Clean
        displayName: 'Clean'
        dependsOn: [  ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Clean --skip'
            env:
              NuGetApiKey: $(NuGetApiKey)
      - job: Restore
        displayName: 'Restore'
        dependsOn: [ Clean ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore --skip'
            env:
              NuGetApiKey: $(NuGetApiKey)
      - job: Compile
        displayName: 'Compile'
        dependsOn: [ Restore ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Compile --skip'
            env:
              NuGetApiKey: $(NuGetApiKey)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: output
              pathtoPublish: 'output'
      - job: Pack
        displayName: 'Pack'
        dependsOn: [ Compile ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Pack --skip'
            env:
              NuGetApiKey: $(NuGetApiKey)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: packages
              pathtoPublish: 'packages'
